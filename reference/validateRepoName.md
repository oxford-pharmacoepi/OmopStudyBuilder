# Validate repository name for GitHub

Checks if a repository name follows GitHub naming rules. Throws an error
with helpful suggestions if invalid.

## Usage

``` r
validateRepoName(name)
```

## Arguments

- name:

  Repository name to validate

## Value

TRUE if valid, throws error otherwise
